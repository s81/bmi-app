name: bmi_calculator
description: Scientific BMI Calculator — WHO & Asian cutoffs, 4 validated indices
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1
  provider: ^6.1.2
  flutter_math_fork: ^0.7.2
  supabase_flutter: ^2.5.6
  fl_chart: ^0.68.0
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
